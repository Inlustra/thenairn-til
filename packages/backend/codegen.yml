schema: "./src/graphql/schemas/*.graphql"
generates:
  ./src/graphql/types.ts:
    config:
      mappers:
        User: ../database/user#UserDocument
        Til: ../database/til#TilDocument
    plugins:
      - typescript
      - typescript-resolvers
      - typescript-operations
